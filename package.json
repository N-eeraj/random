{
  "name": "@n-eeraj/random",
  "version": "1.2.101",
  "description": "This Random package is a simple and flexible utility for generating random values in JavaScript and TypeScript. You can generate numeric, string and boolean values with full control over the generation using parameters.",
  "scripts": {
    "minify": "sh minify.sh",
    "build": "tsc && npm run minify",
    "dev": "ts-node-dev src/dev.ts",
    "test": "npx vitest run --coverage",
    "test-publish": "sh publish.sh"
  },
  "keywords": [
    "random",
    "random js",
    "random generator",
    "shuffle"
  ],
  "author": "N-eeraj",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "terser": "^5.37.0",
    "ts-node-dev": "^2.0.0",
    "vitest": "^2.1.8"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/N-eeraj/random"
  }
}
